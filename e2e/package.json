{
  "name": "legalcase-e2e",
  "version": "1.0.0",
  "description": "Smoke tests E2E — chemins critiques AI LegalCase",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
