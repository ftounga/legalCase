<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="053-create-spring-session-tables" author="legalcase">
        <comment>
            Crée les tables Spring Session JDBC pour la persistance des sessions HTTP.
            Schéma officiel Spring Session — compatible PostgreSQL et H2.
        </comment>

        <createTable tableName="spring_session">
            <column name="primary_id" type="CHAR(36)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="session_id" type="CHAR(36)">
                <constraints nullable="false"/>
            </column>
            <column name="creation_time" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="last_access_time" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="max_inactive_interval" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="expiry_time" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="principal_name" type="VARCHAR(100)"/>
        </createTable>

        <addUniqueConstraint tableName="spring_session"
                             columnNames="session_id"
                             constraintName="spring_session_ix1"/>

        <createIndex tableName="spring_session"
                     indexName="spring_session_ix2">
            <column name="expiry_time"/>
        </createIndex>

        <createIndex tableName="spring_session"
                     indexName="spring_session_ix3">
            <column name="principal_name"/>
        </createIndex>

        <createTable tableName="spring_session_attributes">
            <column name="session_primary_id" type="CHAR(36)">
                <constraints nullable="false"
                             foreignKeyName="spring_session_attributes_fk"
                             references="spring_session(primary_id)"
                             deleteCascade="true"/>
            </column>
            <column name="attribute_name" type="VARCHAR(200)">
                <constraints nullable="false"/>
            </column>
            <column name="attribute_bytes" type="BLOB">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey tableName="spring_session_attributes"
                       columnNames="session_primary_id, attribute_name"
                       constraintName="spring_session_attributes_pk"/>

    </changeSet>

</databaseChangeLog>
