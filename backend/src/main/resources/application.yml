spring:
  application:
    name: legalcase-backend
  profiles:
    active: dev
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:change-me}
            client-secret: ${GOOGLE_CLIENT_SECRET:change-me}
            scope: openid, email, profile
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB
  mail:
    host: ${MAIL_HOST:}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  frontend-url: http://localhost:4200
  mail:
    enabled: false
    from: ${MAIL_FROM:${MAIL_USERNAME:}}
  pipeline:
    direct-analysis-threshold-chars: 600000
    limits:
      droit-du-travail:
        document:
          faits: 5
          points-juridiques: 3
          risques: 3
          questions-ouvertes: 3
          timeline: 0
        dossier:
          faits: 7
          points-juridiques: 5
          risques: 5
          questions-ouvertes: 5
          timeline: 5
      droit-immigration:
        document:
          faits: 7
          points-juridiques: 4
          risques: 4
          questions-ouvertes: 3
          timeline: 0
        dossier:
          faits: 10
          points-juridiques: 6
          risques: 6
          questions-ouvertes: 5
          timeline: 8
      droit-famille:
        document:
          faits: 5
          points-juridiques: 3
          risques: 3
          questions-ouvertes: 3
          timeline: 0
        dossier:
          faits: 7
          points-juridiques: 5
          risques: 4
          questions-ouvertes: 5
          timeline: 5
  stripe:
    enabled: false
    secret-key: ${STRIPE_SECRET_KEY:}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
    price-id-solo: ${STRIPE_PRICE_ID_SOLO:}
    price-id-team: ${STRIPE_PRICE_ID_TEAM:}
    price-id-pro: ${STRIPE_PRICE_ID_PRO:}

sentry:
  dsn: ${SENTRY_DSN:}
  environment: ${SENTRY_ENV:dev}
  traces-sample-rate: 0

anthropic:
  api-key: ${ANTHROPIC_API_KEY:change-me}

storage:
  s3:
    endpoint: ${S3_ENDPOINT:}
    region: ${S3_REGION:us-east-1}
    access-key: ${S3_ACCESS_KEY:minio}
    secret-key: ${S3_SECRET_KEY:minio123}
    bucket: ${S3_BUCKET:legalcase-dev}
    path-style-access: ${S3_PATH_STYLE_ACCESS:true}

server:
  port: 8080

management:
  endpoints:
    web:
      base-path: /api/actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
